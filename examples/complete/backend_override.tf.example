# Local development only. Copy to backend_override.tf (gitignored) to use local state instead of
# the azurerm backend, so you can plan and apply without remote backend access. Re-run
# `terraform init` after adding it. Do not commit it.
terraform {
  backend "local" {
    path = "terraform.tfstate.local"
  }
}
